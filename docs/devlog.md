# BigBonusBlitz 制作ログ

AI と作った過程の記録。**何を決めたか**と**なぜそう決めたか**を残す。
コードの変更内容は `git log` に入っているので、ここには書かない。

書く単位は「決定」。実装しただけのことは書かない。
決定が後で覆ったら、消さずに「覆した」と追記する。判断の経緯そのものが資料になる。

---

## 2026-09-23 冒険の改良を、他AIが実装できる企画へ

本人は戦闘の波、敵20種、顔付き会話、GETの重なり、主人公のスプライトと接続、武器外見、光と影、呪いの常設表示を企画するよう依頼。続けて、他のAIも「どう行えばかっこよく改善できるか」分かる形を指定した。企画の正本を [ADVENTURE_REDESIGN_PLAN_2026-09-23.md](ADVENTURE_REDESIGN_PLAN_2026-09-23.md) とし、要望R01〜R12、変更先、20体台帳、素材契約、工程P0〜P5、検証、担当AIへの指示テンプレートをまとめた。ゲーム機能は未実装。

現状は敵テーブル6件でも描画typeが3系統、OPの全文表示と冒険の文字送りが別実装、GETは会話との高さ回避があるものの描画ルートが異なると確認。原因は実行再現前なので断定しない。20体は既存を含む通常16＋ボス4の提案とし、ボスも専用シルエットと動作を持たせる。戦闘演出の強弱と確率・遭遇分布の変更を分け、派手な演出で勝敗を変えない方針を明記した。

先に共通会話/通知と呪いHUDを整え、森・3敵・2武器の見本でスプライトの基準点や武器の握り位置、描画方式を固めてから量産する案。現背景はRawImageなので、Light2Dを置くだけの改良にせず、専用2D描画をRenderTextureへ合成する方式と低負荷案を比較する。OPは本人が説明を削った29行を維持し、長い初稿へ戻さない。原文はMEMO、現在地はPROJECT_STATE、経緯はAI_HANDOFF_HISTORYへ記録。今回は文書と企画のお知らせdev 317のみで、画像生成・Unity動作/性能/聴感検証は行っていない。

文書検算では20敵（通常16/ボス4）、既存6IDの一致、12要望、6工程、7武器、4呪い、OP8場面29行、正本へのリンク、お知らせJSONと番号重複、差分空白を確認。初回に `BACKGROUND_ART_DIRECTION.md` の不在を検出したため、存在するdevlog/PROJECT_STATEへ参照を修正し、古い案内にも不在と再確認事項を明記した。静的検算は再実行して成功。文書検証をゲームの動作検証とは扱わない。

## 2026-09-23 設定画面と、言われる前に揃えるUI品質

本人は設定画面の紫色・縦長のつまみ・単色の大きな面に対して、白い城塞と紺/金の参考画像へ改善するよう指定。「今後他の箇所のUIでもこのように言わなくても２枚目の質になるよう」と継続要件も明示した。共通基準を `AZURE_UI_QUALITY_STANDARD.md` にし、AGENTS.md/CLAUDE.mdから必読として案内。既存画面を無差別に変えるのではなく、今後作成・改修する対象へ採用済み画風と完成検証を適用する。

背景と白い面は採用済み城塞/カードを再利用し、紋章入りの縦ナビだけ内蔵画像生成で制作。設定値と操作は生UIで維持。ON/OFFは文字・色・位置、スライダーは44pxの操作領域と固定寸法の宝石を分けた。つまみの縦伸びはSlider.UpdateVisualsによるアンカー再設定が原因で、子自身ではなく親HandleAreaの高さを固定すると解消した。

v1は独自GraphicのCanvasRenderer不足で描画に失敗。v2でタイトルの削除説明の行高不足、150%字幕の閉じかぎ括弧だけの折返しを発見。説明領域を広げ、字幕は意味の切れ目で改行。タイトル960幅から設定1170幅を開く点もSafeRoot基準の縮小で対応した。v3の隔離EditModeではTime.timeと確認期限の初期値がともに0のため、既存の削除処理へ進んで失敗。本番には触れていない。正の確認期限を必要にするガードをタイトルと冒険へ追加し、最終検査は第1確認までとした。

v4は操作検査に合格したが、冒険側AUTO条件が古いフラット緑ボタンに戻り、文字が暗いと実描画で判明。3行の共通スイッチへ統一し、状態更新元も共通RefreshSwitchへ変更。入口を模倣した見本だけでなく、実TitleScreen/GameControllerを構築する必要があった。仕様・全プロンプト・版別QAの読み方は `AZURE_SETTINGS_DESIGN.md`。実機タッチ・配布ビルド・聴感は未検証。

最終v5-finalは12状態×2解像度=24枚、passed=true。Core/Runtime/Testsコンパイルと配置検算、差分空白検査も成功。AUTO条件のビット保存、タイトル/冒険の第1削除確認、設定中のBET遮断を検証した。お知らせdev 315。OS実キー送信・実機タッチ・配布ビルド・聴感は未検証。

## 2026-09-22 元気なサリアと装備画面

本人は添付の紺・金の装備UIを希望し、既存フォルダの「元気な子」の立ち絵を複数使いたいと指定。笑顔で手を差し出す姿、ピース、肩に剣、飛び出す姿、手を上げる姿の5種類を目視で選定。原PNGのアルファと縦横比を保持して内蔵し、左右ボタンで循環する。装備選択やページが姿の変更でリセットされないよう、表示更新を独立させた。

生成を担当するのは月夜の城塞背景と金の翼剣パネル。画面の数値、ボタン、一覧と効果比較は既存コードへ接続。見本に描かれた未実装のスキル/覚醒は追加しない。街の装備画面からもステータスへ行って戻れるようにし、冒険側のコールバックも維持した。

v1の検証は装備・解除・売却・まとめ売り・所持上限・工房品保護・5姿・ステータス復帰が通った。実描画で、手を上げる姿と肩の剣が見出し付近にかかり、詳細の単色面が金枠を覆うと判明。姿ごとの位置/倍率を調整し、詳細用の空きパネルを別生成して装飾を保つ。多数効果にはスクロールの案内も追加。正本・全プロンプト・素材対応表: `AZURE_EQUIP_DESIGN.md`。端末タッチ・配布ビルド・聴感は未検証。

v2の長い装備名と多数効果を目視し、唐草に文字が近づく問題を発見。名前の幅204、比較・効果の幅210へ調整した。v3-finalは14状態×2解像度=28枚でpassed=true。Core/Runtime/Testsコンパイルと配置検算も成功。実セーブは書かず、隔離コピーの仮データで検査した。お知らせdev 314に登録。

## 2026-09-22 宇宙のステータス強化

本人は参考画像の三列カードを希望し、背景だけは宇宙と指定。金枠・白い中央面・ライフ/テクニック/ラックの構造を採用し、星雲・惑星・金の星座線を新規生成した。金枠と3紋章も別PNGで生成。効果・ポイント・上限・費用は既存の StatsDirector / JSON を使用。画像内の例示値をセーブへ書き込まない。

ショップと冒険中で同じ StatsScreen を使用するため、戻る文言は「冒険へ」ではなく「戻る」。振り直しに費用確認を追加し、確定時の残高・割当・費用の再確認を行う。キャンセルで消費しない。確認状態を画面デザインの一部として扱った。

最初のUnity実描画では、日本語明朝の行高が30pxの聖域見出しに収まらず文字が出なかった。また見出し領域同士の3px重複を機械検査で発見。行高と間隔を再配分して再描画する手順を残した。詳細・全生成プロンプト・QA条件は `CELESTIAL_STATS_DESIGN.md`。検証結果は版別 validation.json を参照。端末タッチ・配布ビルドは未検証。

最終版 v8-final: 3能力加算、ポイント不足、上限、6桁残高、有料/無料の振り直し、キャンセル、確認時残高変更、ショップ入口と復帰、閉じるを検証し passed=true。1280×720と2556×1179で18枚を保存。Core/Runtime/Testsコンパイル成功、配置検算も通過。目視で項目名と数値を分け、金枠にかからない余白と要約の意味に沿う改行へ修正した。

## 2026-09-22 制作履歴を他のAIとnoteへ引き継ぐ

### 決定: 会話の要約ではなく、事実の種類を分けた正本を残す

本人から「過去の内容も全部」「日付も内容も色んな情報を残して」「他のAIエージェントでもわかるように」と依頼された。会話に正確な日時が残っていない依頼は推測で日付を付けず、「会話上の順序（日時不明）」と記録する。ユーザー要望、実装済み、検証済み、未検証、添付画像の参照をラベルで分けた。

正本は `docs/AI_HANDOFF_HISTORY.md`。noteへ編集しやすい整理版は `docs/note/AI_CHANGELOG_FOR_NOTE.md`。本人の原文は `docs/note/MEMO.md`、記事へ入れる前の材料は `docs/note/MATERIAL.md`。既存のnote第3稿は壊さず、次稿で統合する。

### 決定: 添付画像は視線・面・雰囲気の参考にし、ゲームの数値や文言を仕様にしない

マップとショップの添付画像には、画面の目的、色、余白、三列構造、装飾、視線の流れがある。一方、画像内の残高、商品数、地点名、ボタン文言はデザイン例であり、`game_config.json` と実際のウォレットへ接続する。生成画像へ文字やUIを焼き込まず、Unity uGUIで重ねる。

### 検証: 2026-09-21〜22のショップ一覧

大きな2列カード → 1列縦スクロール → 32px行のコンパクト一覧という順に変更した。本人の「みずらいもっと１０行くらい見れるようにしたい」を受け、9点は全件、19点の隔離データは10行を表示する設計へ変更。`AzureShopValidation.Run` の `tools/azure-shop-qa/2026-09-21/v8-compact/validation.json` は、購入、キャンセル、確認時再判定、補給、10行、19点、ホイール、ドラッグ、キーボード追従、カテゴリ、上限、6桁、長い名前、ステータス、閉じるを `passed:true` で記録。`docs/check_layout.py` は重なり・はみ出しなし。

### 失敗から学んだこと: 画像だけでは再現できない

ショップの生成素材は、外側に背景色が残った金枠を一度不採用にした。採用素材でも、2列カードは美しくても一覧性が低かった。画像生成、uGUIの状態管理、操作検証、実描画を別々の証拠として残す必要がある。

---

## 2026-09-07 ブラウザ版から Unity へ移す

### 決定: コードは捨て、データと仕様だけ引き継ぐ
ブラウザ版は JS 5,766 行。うち移植価値があるのは抽選・滑り制御・エンゲージの約 1,000 行。
DOM 描画と Web Audio の合成音は Unity では別の手段があるので捨てた。
確率テーブルとリール配列は JSON に変換して持ち込んだ（`tools/convert_to_json.js`）。

### 決定: Unity 6.3 LTS + Universal 2D（URP）
2D 標準で、後から Light2D やポストプロセスを足せる。Built-in だと後で移行が要る。

### 決定: 主人公はまおちではなく別キャラ
まおちは広告塔（X アカウント）に専念させる。ゲームの主人公は別に立てる。

### 決定: エンジン部分を Unity 非依存の C# にする
`BBB.Core` は MonoBehaviour を使わない。理由は、確率と制御を単体テストと
シミュレーションで検証したいから。実際この判断が後で効いた（下記の検証はすべて Core を直接回している）。

---

## 2026-09-08 リール制御を作り直す

### 決定: 先読み制御にする
当初はブラウザ版と同じ「スコア最大の停止位置を選ぶ」方式だった。
1,104,000 パターン（23フラグ × 押し順6通り × 押し位置20³）を総当たりしたところ、
ハズレなのに役が揃うケースが 340 件見つかった。

そこで「残りリールがどこで押されても・どの順で押されても詰まない候補だけ選ぶ」方式に変えた。
状態を「各リールの上段コマ番号」で表してフラグごとにメモ化（21³ 状態）。
最初の実装は総当たりで 10 分以上かかったが、メモ化で 6 秒になった。

### 決定: ベル・リプレイは任意ライン、2ライン同時成立は禁止
サブフラグ A/B/C ごとに成立ラインを限定していると、配列の制約が厳しくなりすぎて
変則押しで取りこぼしが出る。A/B/C は抽選の重みだけに使い、どのラインで揃ってもよいことにした。
代わりに 2 ライン同時に揃う停止形は制御で避ける（払い出しが 2 倍になるのを防ぐ）。

### 決定: 左リール [14]CHERRY ↔ [15]REPLAY を入れ替え
上記の変更でも、変則押しでベル・リプレイが 5% 取りこぼれた。原因は左リールの
チェリー配置。非チェリー時は左枠内にチェリーを置けないため、左の選択肢が減る。

1 リール内の 2 コマ入れ替えを全探索したところ、全役・全押し順で 100% になる配列が
2 つ見つかった。うち「[14]↔[15]」を採用。

### 決定: CHANCE_D（中段 RP/RP/★ のチャンス目）を廃止
配列を変えても取りこぼしが残った一因。停止形を持たせず、リーチ目扱いにした。

### 決定: ライン番号は「0=上段, 1=中段, 2=下段」
ブラウザ版はコメントとコードが食い違っていた（コメントは中段、コードは上段）。
コメント側を正とした。

### 決定: 始動ウェイトは 2.05 秒。リールは即回転
実機は 4.1 秒だが、遊んでいて長い。半分にした。
**当初はウェイト中リールを止めていたが、本人の指摘で「回転はさせて停止だけ禁止」に変えた。**
実機と同じ挙動で、待たされている感じが減る。

### 決定: エンゲージは案B「潜伏当否型」
4 案（HP削り / 潜伏当否 / 継続ゾーン / ボス・レア敵）から選んだ。
1G目=敵の色、2G目=セリフ、3G目=停止音で段階的に示唆する。
案A（HP削り）はボーナス中に回す予定。

### 決定: 1〜2G 目は敵が死なない
ベルが揃っても敵を消さない。決着は 3G 目終了時の判定だけ。
「揃ったのに死なない」ではなく「まだ判定していない」と見せるため、
ヒットと砂煙だけにして、潰れる演出は 3G 目に取っておく。

### 決定: 敵は前兆 3G を経てから出る
唐突に出るのをやめた。当選G から 3G かけて、画面が暗くなり、
右端から影が近づき、キャラが足を止める。

---

## 2026-09-09 AT・冒険・ショップ（別 PC で実装）

### 決定: AT は候補5「ゾーン型」
AT 当選 → 洞窟を見つける → 洞窟内で AT。番長（押し順ベルで枚数）と
モンハン（HP を削って討伐）の融合。討伐で G 数上乗せ。

### 決定: すべての確率はテーブル（game_config.json）で持つ
コードに確率を直書きしない。新しい演出を足すときは必ず対応するテーブルを作る。
本人の指示。後から調整するときにコードを触らずに済む。

### 決定: 持ち越しボーナスの引き込みを 20 コマにする
「成立中なのに揃えられない」という指摘が起点。4 コマ引き込みで実測したら
BB_A が 2%/G しか揃わなかった。`bonusPullInSlip` で実機どおりに戻せる。

---

## 2026-09-10 技術介入・通貨・スプライト

### 決定: Lv / ソウル / エンバーの役割を分ける
- Lv … ベースステータス
- ソウル … スキル・バフ・装備の改造
- エンバー … ライフ・松明・満腹度・装備の購入（自分の状態を整える）

**この整理は本人が最初から持っていたが、実装が追いついていなかった。**
Lv は貯まるだけで何にも効いておらず、エンバーは存在すらしていなかった。
「なぜ分からないのか」と言われたのは、記録だけ見て実装を見ていなかったため。

### 決定: 技術介入は 3 種すべて入れる
ビタ押し・2コマ目押し・ミッションフラグ。対象は実機経験者も初心者も。
オート中は出さない。失敗してもマイナスは無く、難易度で報酬が変わる。

### 決定: 制御の滑りで取れないゲームは課題を出さない
最初の実装では、狙って押しても成功率が 75% しかなかった。
残り 25% は「制御が滑らせたせい」で、腕の問題ではない。

そこでレバーオン時に「その狙い位置で押したら成功するか」を先に確かめ、
無理なら課題自体を出さないことにした。結果は 100%（狙って押した場合）と
50%（適当に押した場合）。失敗の理由が腕だけになった。

### 決定: 補給（松明・路銀）の支払いをソウルからエンバーへ
役割分担に合わせた。あわせてエンバーの入手経路を作った
（討伐・逃走・AT バトル・章クリア・初訪問・ボーナス終了・宝箱）。
それまでエンバーは技術介入でしか手に入らず、初期値も 0 だった。

### 決定: 主人公のドット絵は 192x192
表示が 206px なので、これ以上ドットを増やしても画面で潰れる。ここが効く上限。
当初 96x72 で作ったが粗すぎた。

### 決定: 生成は ComfyUI、ドット化は Python
ComfyUI 単体では「一貫したキャラの多ポーズスプライトシート」は作れない。
コマ間で髪の房や剣の形が変わり、ドット絵指定してもピクセル格子に乗らない。

そこで役割を分けた。ComfyUI は 1 枚ずつ高品質な絵を出す係。
背景除去・身長合わせ・減色・ドット化・シート化は Python でやる。
ポーズは OpenPose 骨格（自前生成）で固定し、キャラは IPAdapter で設定画に寄せる。

### 未決
- 生成スプライトを採用するか、手描きドット絵で行くか

**覆した**: 「Lv が上げるベースステータスの中身」は 09-10 中に決着していた。
ライフ / テクニック / ラックの 3 系統で、ショップ画面から振る。未決欄の書き漏らし。

---

## 2026-09-10 主人公の HP

### 決定: 案A（HP削り）はやめ、ボーナス中は HP が回復する場にする
当初は「ボーナス中に敵の HP を削る」つもりだった（09-08 に案A として保留していたもの）。
本人の判断で逆にした。ボーナスは削る場ではなく、通常時に減ったぶんを取り返す場にする。
ベルが揃ったときに一定確率で回復する。

### 決定: HP が松明を置き換える。資源は 1 本にまとめる
松明（本数）と HP（バー）を別々に持つと、通常時に管理するものが 3 つ（エンバー・松明・HP）になる。
HP を「あと何G歩けるか」そのものにして、松明は廃止した。
ショップで買うのは回復薬で、1 個買うと HP が 1 個ぶん回復する。

内部は松明の持ち方（個数 + 使いかけの残り）をそのまま使っている。
見た目は 1 本のバーだが、ショップで買う単位が要るため。
1G で 1 減るので、合計値がそのまま残りG数になる。**この対応があるので過去の収支検証は生きている。**

### 決定: HP0 は「力尽きた」扱い。章の最初へ戻る
それまで松明切れは進行を残して帰還、エンバー切れだけが力尽き、と罰が 2 段階だった。
HP にした以上 0 は死。エンバー切れと同じ扱いに寄せた。

### 決定: UI の枠とアイコンを画像にする。板は紺、文字は白のまま
本人が ChatGPT で出した UI シート（白×金×紺）を使う。タイトルが空と城の世界になったのに、
本編だけ黒鉄のままで遷移すると世界が変わって見えていた。

シートには白（クリーム）の板もあるが、採用したのは紺の板だけ。
白い板にすると全画面の文字色を黒へ反転する必要があり、変更が UI 全体に広がる。
紺なら既存の白文字がそのまま読めるので、`UiSkin` の中だけで差し替えが済む。
白い板は切り出して置いてあるので、画面ごとに文字色を面倒みられるようになったら使う。

縁の幅は `.meta` ではなくコード（`UiSkin.FrameBorders`）に持たせた。
別 PC で pull したとき Unity の取り込み設定に左右されず、同じ見え方になるため。

### 決定: 画面の配置はコードの定数ではなく JSON（ui_layout.json）に持つ
枠を絵に替えたら配置の粗が一気に見えた（金縁が 15 個並ぶ、飾りが小さいボタンを食う）。
直すたびに Unity を開いて Play するのは重いので、ブラウザの配置エディタで動かして保存し、
Unity は次の Play で JSON を読む形にした。ファイルが無ければコードの既定値なので、消せば元に戻る。
枠の絵の選択（絵 / 手続き / どの絵か）も同じ JSON に持たせ、要素ごとに切り替えられるようにした。

**残している制約**: リール窓の大きさは図柄に合わせた固定値、表示域（背景）はステージ札に付いて動く。
どちらもコードの定数が他の計算に使われているため。

### 決定: 力尽きたら HP は初期値まで戻す
戻さないと HP0 のまま街に着き、回復薬を買うエンバーが無いと二度と出られない。
死亡処理の中で戻すようにした。**罰を厳しくしたときは、詰む経路が無いかを併せて見る。**

---

## 2026-09-10〜11 販売 note（制作過程の記事）

記事本体は `docs/note/`。案件の現在地とタスクは外部知能 `projects/note-bbb.md`。ここには決定だけ。

### 決定: 構造は「仕組み」+「失敗の全開示」。読者は非エンジニア。980〜1,500円
5案（時系列ドキュメンタリー / 運用マニュアル / 失敗カタログ / 連載 / 混合）から本人が選択。
「AIでゲーム作ってみた」は飽和しているので、実物（指示書・記憶ファイル・失敗記録）を主役にする。
公開はゲーム完成と同時。それまで材料を貯める。

### 決定: 記録は devlog に一本化。note 側は割り当て表と本人メモだけ持つ
`docs/note/MATERIAL.md` に決定や数字を書き写さない。記事がどの材料をどこで使ったかの対応表に留める。
本人の言葉は `docs/note/MEMO.md` に原文のまま置き、AI は言い換えない。

### 決定: 数字は「いつ・どの条件で測ったか」つきでしか記事に出さない
機械割 163.7% と 129.7% の食い違いは条件差ではなく測った時点の差だった（あいだに AT 調整）。
記事には 2026-09-09 の 200万G 実測（設定1・ナビ従 132.6% / 順押し 107.7%）を日付つきで採用。
20万G だと 128.8〜134.1% に揺れた件は、そのまま「何回まわせば足りるのか」として本文に使った。

### 決定: 作業時間は本人の実感値で書く。今から計測はしない
平日は仕事の合間の1日2時間ほど。105日のうち約2ヶ月止まっていたので、実際に触ったのは約50日。
費用は記録が無いので「測っていない」と本文に明記。

### 覆した: 「使っている AI のモデルは5月から変えていない」
AI が推測で断定して書いた。本人のメモ「ちょくちょくアップデートが入るので、最新モデルで
やりたいことができるのがいい」で覆った。「同じ更新を受けながら3ヶ月止まっていた」に書き換え、
仕組みは AI の改善を受け取るためのもの、という位置づけにした。

---

## 2026-09-11 タイトルの窓とお知らせ

### 決定: 設定・お知らせの窓はクリーム地（panel_cream_sm）+ 紺のタブ（pill_navy_sm）
紺の板（panel_navy）は隅の飾りが大きく、題が隠れて読めなかった。frames_V2 の
panel_cream_tab と同じ組みを、伸びる板とピルで組んで作る。クリーム地なので文字は紺（#2b2f45）。

### 決定: お知らせには「直した内容」を出す。修正のたびに news.txt を更新する
`Resources/Data/news.txt` を日付ごとに追記する。プレイヤー向けの言葉で書き、コミット名は写さない。
更新を忘れると窓が古いままになるので、タイトルに触る修正は news.txt もセットで直す。

### 決定: 街の画面は GPT-6 が組んだ版を採用。Claude 版は捨てる
Claude 版は枠を全部に付け、文字を全部中央揃えにし、数値を 1 行の文章に埋めていた。GPT-6 版は
枠を押せる場所と主役の板だけにし、数値を「アイコン + 小見出し + 太い値」の札にし、主ボタンを桃 1 つにした。
決め手は確かめ方の差: Claude は HTML の近似とコンパイルで「反映した」と言い、GPT-6 は実際の uGUI を
batchmode で PNG に描いてから納めた。差分表は `game-design` スキル §8.2、プロジェクト側は `docs/ui_rules.md` 9・10。

### 決定: 画面を組んだら実機描画の PNG を見るまで完了にしない
`Assets/Editor/MapUiValidation.cs` の型（分離 batchmode・2 解像度・タッチ 44・validation.json）を以後の画面でも使う。

---

## 2026-09-12 お知らせの形式と冒険画面の幅

### 決定: お知らせは「日付・題」の一覧にし、押すと中身を出す。V1 で窓ごと消す
`Resources/Data/notices.json` に日付・題・中身で足していく（新しいものを先頭に）。開発中の遊びなので、
正式版（V1）になったら窓と JSON を消す。news.txt はこの形式に置き換わったので、不要になった。

### 決定: 冒険画面は iPhone 幅（1170×540）で組む。左の表示器は LIFE / EMBER / PAYOUT の順
本人がモックを見て「表示域をもっと広く」を選んだ。STOP の帯までリールを下げ、設定・グラフは右下の隅のアイコン。
LIFE は本人の指示で一番上に。16:9 の画面では舞台ごと 0.82 倍に縮む（切らない）。

### 覆した: ボーナス中のベルを 11 / 3 / 8 枚にしたら REG が終わらなくなった
純増でボーナスを終える型なので、順押しだと REG の期待値が投入を下回り終わらない（下の検証の記録）。
候補「外しも 8 枚」「終了条件を払い出し合計に」「RB のベル率を上げる」「G 数管理」を実測つきで出した。

### 決定: ボーナスは G 数で終わる（BIG 60G / REG 30G）。規定枚数に届かなくてよい
本人「届かなくていいよ。取り切ったら終了で、ゲーム数にしよう」。規定枚数（純増）に先に届けばそこで終わり、
届かなくても G 数を使い切れば終わる（擬似ボーナスの型）。G 数は `bonusGames`（JSON）。
実測（冒険なし・300 回・設定1）: BIG 59G で純増 +271（順押し +133）/ REG 30G で +23（順押し −12）。
設定5 の REG は −27。REG のベル率（36.6% / 設定5 は 21%）を上げるかどうかは別途。
→ 更新（09-14、やること棚 t04）: RB 表のベルを 65% に（全設定同じ）。純増は規定 90 枚が上限なので +100 には届かず +88。+100 前後にするなら payouts.REG（規定枚数）を 110 前後に上げる（未決）。

### 決定: 装備の枠は 8 つ（頭 / 体 / 手 / 足 / 武器 / アクセ 1〜3）。効果にステータス +n を足す
本人「ステータスアップの装備アイテムが欲しい」「装備できる箇所は頭、体、手、足、武器、アクセ 1.2.3」。
品の種類とは別に「着ける枠」を持ち、アクセだけ 3 枠。空き枠が無ければ一番弱い品と入れ替える。
ステータス効果（statLife / statTechnique / statLuck）は装備だけが持ち、振ったポイントに足して使う。
ベースは 40 種（種類ごとに 6〜9）、接辞 16 種。数はすべて game_config.json の equipment。

### 決定: レア度は 9 段階（白 青 黄 緑 赤 紫 黒 金 虹）。名前は コモン〜プリズム
本人の指定は色の並び（パチスロの期待度の色）。名前は本人が「コモン、アンコモンでは足りない」と言ったので
コモン / アンコモン / レア / スーペリア / エピック / ミシック / レジェンド / ディヴァイン / プリズム を当てた（JSON で変えられる）。
黒は「レジェンドはイメージが違う」で アビス（深淵）に替えた（案 エクリプス / アビス / オブシディアン から本人が選択）。
深さ 1 でコモン 60%、深さ 8 でアビス以上 1.6%（`docs/equipment.md` の表）。黒の文字色は紺の板で読めるよう灰紫（#7c7f99）。

### 決定: 実績（トロフィー）はプレイヤー単位で永続。解除でソウル
本人「実績とかのトロフィーのやり込み要素」。数えものは `AchievementState`（セーブに JSON 文字列で持つ）。
一覧は `achievements.json`（26 個）。判定は `AchievementDirector.Track` を毎回転の Evaluate 後に呼ぶ。

### 決定: 第 2 章は同じ地図を二度目に通る話。問いは「灯を分けると、自分の灯は減るのか」
第 1 章の結末（竜は灯を抱えていただけ。街に持ち帰って分ける）を受けて、分けた灯が世界をどう変えたかを
同じ 8 段で書く。地図とステージ名は共通（章ごとの地図はまだ無い）。第 2 章の頭では周回の台詞を足さない。
第 3 章以降は未作成で、第 1 章の台詞と周回の台詞に戻る。

### 決定: 落とし物は出どころ（敵 / ボス / 狩猟 / 宝箱）ごとの表（`drops`）で持つ
本人「敵から、宝箱から、ボスから装備やいろいろドロップするようにして」。装備は 1 つまで（率）、装備以外は
1 つずつ独立に率で落ちる（ソウル / エンバー / 回復薬 / AT の地図 / お守り / EXP）。ボスは装備 100% と深さ +3。
equipment.dropRate* は残してあるが、drops があればそちらを使う。

### 決定: 図鑑（装備 / お宝）は実績の窓のタブに入れる。拾った物だけ名前が出る
タスク棚の t01〜t03（2026-09-13、本人がチェック）。窓を増やさず、★ の窓を「実績 / 装備の図鑑 / お宝の図鑑」の 3 タブにした。
拾った回数は実績の数えもの（`seen:<baseId>` / `treasure:<id>`）で持つので、セーブの形は変えない。
未入手は「？？？」と「深さ n から出る」だけ見せる（どこで拾えるかの手掛かりは残す）。
装備の効果は `EquipDirector.EffectDesc` の 1 行説明を装備画面の詳細と図鑑の両方に出す。

### 決定: スランプグラフは潜行をまたいで続く。履歴は潜行ごと
タスク棚 t08（2026-09-13）。波形は `bbb_graph_v1`（PlayerPrefs、セーブ本体とは別キー）に 10G ごとと街へ戻るとき・終了時に置く。
街で増えたエンバー（宿の補填）は回転数を増やさず段差として描く。「前回までの冒険」の一覧は潜行の始まりの点を覚えておいて
そこからの分だけを残す（`SlumpGraph.BeginRun` / `RunSnapshot`）。「ここから取り直す」とセーブ削除で波形も消す。

### 決定: 敵の種類はワークフローのバリアント（A=スライム / B=ゴブリン / C=コウモリ）で選ぶ
タスク棚 t10（2026-09-13）。原因: `workflow_config.json` の ENEMY のバリアントが全部 0 → 結果が "NONE" → バリアント "A" のスライムは
選ばれず、未指定（ゴブリン / コウモリ）だけが出ていた。高確ステージの追加出現は "A" 固定なのでスライムだけ。
直し: ゴブリンを B、コウモリを C にして、ENEMY を A 40 / B 30 / C 30 に。バリアントの合計は 100 か 0（テストの規則）。
測定: 30 万G（設定 1、seed 11）で 段 A: スライム 218 / ゴブリン 150 / コウモリ 120。直す前は 段 A・B でスライム 0。

### 決定: 装備は捨てずに売る。売値はレア度の表 × 深さ
タスク棚 t11（2026-09-13）。`rarities[].sellSouls`（コモン 5 / アンコモン 12 / レア 30 / スーペリア 60 / エピック 120 / ミシック 250 /
アビス 450 / ディヴァイン 800 / プリズム 1,500）に深さ 1 ごと `sellDepthPercent`（15%）を上乗せ。
目安: 雑魚 1 体のソウルが 10、店の品が 120〜400 なので、コモンは拾い集めても店の足しにならず、レア以上で初めて意味が出る値にした。

### 決定: AUTO は ON / OFF のトグル。速さは別のボタン（x1〜x6）で選ぶ
本人（2026-09-13）「Auto ボタンはただのトグルボタンで、スピードは別の切り替えで行いたい」。
右下の AUTO を 96 幅に縮め、右に 50 幅の速さボタンを置いた（x1→x6→x1 と一巡、AUTO 中に変えれば即反映、値は残る）。
キーは A = AUTO、S = 速さ。スペース長押しの一時オート（常に x1）はそのまま。

### 決定: 技術介入の課題が出たGは「Ready？」を大きく出し、1.2 秒は止められない
本人（2026-09-13）「技術介入が出たら画面に大きく Ready？と出して準備させてほしい」。
レバーオン直後に帯（SlamTitle）で Ready？、その間 `_stopUnlockTime` を延ばす。秒数は `tech.readySeconds`（1.2）。
帯が消えるころに「GO！」。課題そのものはリールの上の帯に今までどおり出る。オート中は課題が出ないので影響なし。

### 決定: ベルナビは「1」を 1.15 倍、候補の「?」を 0.72 倍・少し暗く
本人（2026-09-13）「1 はスケールアップして最初コレを打つ、2 番以降はスケールダウンして色も落として差別化」。
奥行きの表（`NaviDepthScale`）を 1.0 / 0.85 / 0.7 → 1.15 / 0.72 / 0.6 に。候補は絵の色も 0.8 倍、光は半分。
回復の数値は主人公の頭の上に「+n」で出す（バーの上だと気づきにくい、と本人）。
バッジの拡縮の軸は下端（本人: リールに被せたくない）。紋章と丸の pivot を (0.5, 0) にして、揺れは下端基準で置く。

### 決定: 起きたことは「説明」の吹き出しで 1 つずつ言う。SOUL / EMB は文字でなく絵
本人（2026-09-13）「回復したり敵を倒したり、いろんなことが起こるたびにセリフの枠みたいに吹き出しで説明してほしい」
「SOUL や EMB はアイコンで表記して」。`ExplainEvents` が GameResult から文を集め（先頭 3 つまで）、青いタグ「説明」の
吹き出しで流す。物語・旅人の会話中は出さない。アイコンは `IconText`（"{soul}+90" の印を絵に置き換えて 1 行に並べる）で、
上のメッセージと帯（SlamTitle）の両方に使う。吹き出しのアイコン入りの行はタイプライタにしない。

### 決定: エンゲージ中の帯は Web 版の表現（斜めの青い帯に文が流れる）に寄せる
本人（2026-09-13）「小役を揃えて AT のチャンスの帯の出し方は Web 版の表現方法がいい」。Web の `#enemy-banner-top/bottom`
（rgba(0,76,255,.6)、−7°、白太字に黒縁、marquee）を `MarqueeBand` で写した。文は「{EngageMaxSpins}ゲーム以内に小役を引ければ...CHANCE!?」。
出現時に出して HideEnemy で消す。帯に RectMask2D は付けない（回転した RectMask2D は親の Mask と重なると中身ごと消える）。

### 決定: AT のセット終了は 8G の「継続ジャッジ」を挟む。結果は突入時に決め、示唆の色で匂わせる
本人（2026-09-13）「セットG数を超えると継続ジャッジパートに行ってほしい。8G ほど。示唆がちらほら出て最終的に継続するかの内容」
「実機の継続ジャッジと同じ型（白＜青＜黄＜緑＜赤＜金＜虹）」。案 A（突入時に結果を決める）で実装。
`at.judgeSpins` 8 / `judgeHintRate` 55% / `judgeHints` は継続・終了ごとの色の重み（金・虹は終了で 0）。
ジャッジ中は AT の払い出しは続き、狩猟と特化ゾーンは起きない。上乗せがあれば結果に関わらず継続。
測定（harness 100 万G、2026-09-13）: ジャッジの 8G ぶん AT の払い出しが増え、冒険ありの機械割が 103.3% → 106.6%。
釣り合いを戻すなら setSpins を 50 → 42 前後に落とすか、ジャッジ中のナビベルを共通ベル扱いにする（未決）。

### 決定: ベル択ナビの正解は第三停止まで見せない
本人（2026-09-13）。外した第二停止は「ベルを揃える制御のまま、第三停止でこぼせる位置だけから選ぶ」（`SlipController.Stop` の
候補絞り版 + `IsFeasibleFrom`）。第三停止は `bellCommand.missSlip`（8 コマ）まで滑らせてこぼす。
測定（4000 回、seed 3）: 第二停止にベルが見える率 外し 87.3% / 正解 86.4%、外して揃う事故 0（滑り 4 のままだと 7%、絞りなしなら 1%）。
UI は成功・失敗の演出と ○/× を全リール停止後に出す。
→ 覆した（同日）: 本人「見えていても構わない。第三停止で音を出して、第二で出すな、ということ」。制御の変更（候補絞り・missSlip）は戻し、
音と演出・○/× を第三停止に遅らせる部分だけ残した。

### 決定: リールの図柄は tools/symbol_choice.json から組み立てる（元絵 → 余白カット → 図柄ごとの大きさでコマに収める）
本人（2026-09-14）「red7 1、white7 5、bell 8、watermelon 3、cherry 1、大きさ 92%。赤白7 と BAR は他より大きく見せたい」。
元絵は assets/symbols（git には入れていない。1 枚 1〜2MB）。`tools/symbol_build.py` が JSON どおりに 320×146 の透過 PNG を
Resources/Art/Symbols へ書く。大きさは 7 と BAR が 100%、他は 92%。BAR・リプレイ・ブランクは旧絵（tools/symbol_src）の背景を
透明にして使う。選び直しは `tools/symbol_viewer.html`（実配列で回して見比べ、JSON を吐く）。
→ 更新（同日）: 本人がビューアで決めた組み合わせに差し替え。red7 90% / white7_5 85% / bar_12 96% / bell_7 74% / watermelon_3 70% /
cherry_1 66% / replay_6 81% / blank_6 73%。旧絵（symbol_src）は使わなくなった。

---

### 決定: 「n EMB 獲得！」の数字は金文字の絵。効果は「型 × 足せる効果」の 2 層で持つ
本人（2026-09-14）「数字が増えていくのは追加のエフェクトにして ON/OFF」「ドロップシャドウやいろんなエフェクト足せれるように」。
動きの型（slideRL など 10 種）は 1 つ選ぶもの、数え上げ・落ち影・縁取り・桁のポン/ゆらゆら・光の脈・後ろの炎は型に重ねる bool。
すべて `reelFx.emberGain`（game_config.json）にあり、`tools/fx_viewer.html` が同じ式で再生する（ゲーム側 `EmberGainPose` / `EmberGainPhase`）。
数字の絵は assets/symbols/number（gold 0〜9 各 5 候補。silver は 0〜2 のみで未使用）→ `tools/text_choice.json` → `tools/text_build.py`
→ Resources/Art/UI/Text/num_0..9（高さ 128、余白カット）。絵が 1 つでも無ければ文字に戻る。
候補の選び直しはビューアの「数字と「獲得」の書体」。既定はどの数字も候補 1。「獲得」も同じ表（words.kakutoku）で選ぶ。
→ 追記（同日）: 数え上げは「止まった瞬間からのオフセット（countOffset 秒）と長さ（countSeconds 秒）」で持つ。入る動きの長さに縛られない。

---

### 決定: 通常時のモード表 B / C / D はハズレを削ってレア役に回す（棚 t05、案 1）
本人が棚のチェックを入れ直した（2026-09-15）ので、提示していた案 1 で進めた。B / C / D はハズレを総数の 1% / 2% / 3% 削り、
チェリーとスイカに今の比率で足す（D で cherry+suica 4.7% → 7.7%、約 1.6 倍）。A はそのまま。全設定同じ差。
ボーナス確率は動かしていない（案 2 は不採用）。実測（ハーネス 20 万G・設定 1）: 機械割 108.4% → 108.3%。
効くのは高確ステージ（stage.mode = "D"）と、ボーナス後のモード移行（modeTransitions）で B〜D に入ったとき。
→ 覆すなら: 数字は game_config.json の probabilities_B / C / D。案 2（D のボーナス 1.5 倍）に切り替えるときは別途測る。

### 決定: 素材の著作表記（音楽：魔王魂 等）はエンディングに出す（2026-09-21）
魔王魂は表記が必須。本人「エンディングに出すのでまだ ED は作ってないので保留」。表の実体は docs/CREDITS.md。
エンディングを作るときにそこから載せる。それまではストアや配布物にも出さない（配布しない前提）。

### 決定: 効果音も JSON（se_config.json）で素材・音量・高さを決め、ビューアで場面を見ながら選ぶ（2026-09-20）
本人「効果音を設定するビューアを作成してください。演出や場所等々映像があるとわかりやすい」→ 案 1（一覧 + 試聴 + 差し替え）。
効果音の一覧（キー・既定の素材名・音量・合成音・場面の説明）は AudioManager.SeDefs の 1 か所。ビューア（tools/se_viewer.html）は
se_sync.py がそこから読んで作り、合成音は Unity バッチ（SfxProbe.ExportSynth）で tools/se/synth に wav を出して試聴する。
場面の絵は描き出しの切り抜き（tools/se/pics）。素材の候補は assets/sounds に置く（bgm は除く）。
→ 覆すなら: 音の追加は SeDefs に 1 行足して se_sync.py。素材の名前は se_<キー>（元からある素材は上書きせず _pick を付ける）。

### 決定: 章ごとの地図は「同じ形でステージ名だけ変える」。名は story.chapters[].stageNames（棚 t17）
本人がチェックを入れ直した（提案: 第 2 章は同じ形でステージ名だけ「分けた灯」の後の世界に書き換える。枝は同じ）ので、そのまま。
地図の形（30 ステージ・枝・条件）は章で変えない。変えるのは名だけで、無いステージは adventure の名のまま。
街の地図の見出しは adventure.chapterName（第1章  はじまりの草原）でなく story の章の題にした（第 1 章は「灯を継ぐ」に変わる）。
第 3 章の名はまだ付けていない（第 1 章の名のまま。候補に積む）。
→ 覆すなら: 形も変えるなら adventure.nodes を章ごとに持つ必要があり、Core の AdventureConfig から作り直し。

### 決定: 実績の絵はアイコンのシート（Art/UI/Icons）から種類ごとに選ぶ。SDF は描かない（棚 t16）
案 1 は「SDF で 6 種を描く」だったが、別セッションのアトリエ画面でアイコンのシート（44 枚）が入ったので、そちらを使う方が絵が揃う。
回転 refresh / 技術介入 sparkle / ボーナス star_gold / 討伐 swords / 呪い chain / 冒険 compass / 装備 bag / 宝 gift / 魂 crystal。
絵は achievements.json の icon で 1 個ずつ差し替えられる（空なら counter から `AchievementIcons.Of`）。隠し実績は解除まで錠前。
→ 覆すなら: 案 2（GPT で 26 枚描く）に切り替えるときは icon にその絵の名前を入れるだけ。

### 決定: 第 3 章は「灯を持たない者と、どう歩くか」（案 1）。題は「灯の歩幅」（棚 t15）
本人はチェックを入れ直しただけで番号を選んでいないので、先頭に置いていた案 1 で進めた。案 2「分けた灯が消えたとき、誰が責めを負うか」/ 案 3「灯を求めて来た竜に、何を渡すか」は不採用（覆すなら story.chapters の 3 を書き直す）。
第 2 章 B で「すれ違った灯を持たない旅人」と、今度は並んで歩く。同行者は喋らない（話者は主人公と【声】だけ）。
答えは言い切らず、H と踏破の台詞で「灯を低く持つ・隣の歩幅で帰る」を歩き方として出す。
書き方: 3 つの切り口（同行者 / 街の側 / 過去の自分）で書いて 3 観点で採点し、勝った「同行者」に他案の行を移植。反証で出た 6 件（第 1 章の丸写し・同じ段で同じ出来事・言い回しの連続）を直してから入れた。
→ 指摘（直していない）: 枝が 1 本の段（A / H）は真ん中の台詞しか出ず、枝 2 本の B は両方「上」扱い（highBranchMax=2）。第 1〜3 章とも上と下の台詞が出ない段がある。出し方を変えるなら Story.ToneOf。

### 決定: 設定差はボーナスとレア役だけで付け、設定 1 の行を土台に 6 へ向けて補間する（棚 t06）
2026-09-15 に測ったら逆転はもう無く（t04 / t05 のあと）、代わりに設定 2〜5 が全く同じ表で、設定 6 だけ A 表のベルが 20%（他は 1.2%）という形だった。
本人はチェックを入れ直しただけで案は選んでいないので、次で進めた: 通常時 A〜D の各設定は設定 1 の行を土台にし、
BB_A ×2.0 / BB_B ×1.5 / BB_C ×1.2 / RB ×1.5 / レア役（チェリー A・スイカ A B・チャンス）×1.25 を設定 6 の目標として、
設定 2〜5 は重み 0.15 / 0.3 / 0.5 / 0.72 で補間。ベル・リプレイは全設定同じ。モードの振り分けは設定 3 から少しずつ D 寄り（6 は今のまま）。
実測（冒険なし・順押し・100 万G・seed 固定）: 設定 1 106.3% / 2 109.1 / 3 111.0 / 4 113.5 / 5 115.5 / 6 117.4%。
前は 1 106.3 / 2〜5 107.9 / 6 118.7。ナビ従だと 171 → 188%（AT が全 G の 55〜61% を占めるため。これは別件）。
→ 覆すなら: 生成は tools/settings_ladder.py（倍率と重みを変えて --apply し、測り直す）。設定 6 のベル増しを戻すなら probabilities_A の "6"。

### 決定: 技術介入は「押した精度」でランクを付け、成功報酬に % で上乗せする（案 A: Core で判定）
本人（2026-09-14）「ビタのタイミングでランクがあり、ランク報酬で上乗せ。2コマ目押し・1コマ目押し・ビタ押し。ビタは 5 種類くらい、最高 Perfect!! 最低 cool。文字を乗せたい」→ 案 A を選択。
判定は Core（`TechDirector.Rank`）: 押した瞬間に上段にあったコマが狙いの何コマ手前か（0 = ビタ / 1 / 2。3 以上は制御で入ったのでランク無し）と、
ビタの中はコマ内の位置のずれ（|phase − 0.5|）で 5 段。表は `tech.ranks`（名前・色・幅・上乗せ %）。既定は Perfect!! +100 / Excellent! +70 / Great! +50 /
Good +30 / Cool +20 / 1コマ +10 / 2コマ 0。上乗せは成功報酬（souls / embers / exp / atGames）それぞれに %。
押した瞬間は Input System のイベント時刻（`PressTimer`）と直前の Update からの外挿（`ReelView.PressPosition`）で 1 フレームより細かく出す。
リールは 1 コマ 37ms で 60fps だと 2〜3 フレームしか無いので、フレーム単位のままだと 5 段が運になる。
ハーネス（押す位置と位相を乱数、250,000G × 4）: 出題 約 11,000 / 成功 約 6,000、ランクは 1コマ 1,900 / ビタ 5 段 各 400 前後 / 2コマ 800 / 無し 1,300。
→ 未決: 上乗せの量が機械割に効くのは AT 中の atGames だけ。ソウル・EXP の上乗せは進行の速さに効く（測っていない）。
本人（同日）「技術介入やエンゲージで入る EXP も全部 GET のモーションを採用したい」→ 得たもの（ソウル / EXP / エンバー / AT G 数）は
同じ帯（reelFx.emberGain の設定）で出す。
→ 更新（同日）本人「順番に表示ではなく、一気に縦に並べて」「縦に全部の場合、スケールの調整を行いたい」: 同じ G のものは 1 組にして縦に並べ一気に出す。
中心 stackY / 間隔 stackGap / 大きさ stackScale（既定 0.7）/ 収まらなければ縮める stackFit。組と組は前のが抜けてから。

---

## 2026-09-14 街の地図（アトリエ風）の組み直し

### 決定: 指摘は直す前に 3 か所へ書く（ui_rules / game-design / OBSERVATIONS）
本人「ここらへん僕に指摘されたら全部メモって今後起こらない等に最適化すること」。今回の 3 点
（装飾が無く区別が付かない / ホイールでズームできない / 下のボタンが端に張り付き）は
`docs/ui_rules.md` 11〜13、game-design §8.3 / §11.4、O-022 に置いた。

### 決定: 街の地図はフラットのまま、面 3 段・地点 4 段・主ボタン 1 つ・数値は札で組む（案 2）
案 1（操作と余白だけ）/ 案 2（+ 地点の状態と札）/ 案 3（+ 金縁の絵）から本人が 2 を選んだ。
地図は ドラッグ = 移動 / ホイール・ピンチ = 拡大縮小（0.5〜2.0）/ 「現在地」で戻る。
確かめ方は `Assets/Editor/AtelierMapQa.cs`（分離 batchmode・2 解像度・タッチ 44・下端 24・文字の重なり）で、
結果は `tools/atelier-map-qa/`。HTML の近似は使っていない。

---

## 2026-09-14 販売 note 第3稿と「描き出しを残す」

### 決定: 見た目を変えたら描き出しを1枚残す（本人指示）
QA 用に batchmode で描いた PNG（`tools/<画面>-qa/`）と、生成の見本（`docs/art/<日付>/`）を消さない。
scratchpad に描いたものはセッションを終える前に写す。本人のスクショより時系列が細かく、
不採用案や修正前の画面が残るため、制作過程の記事がそこから拾う。ブラウザ／デスクトップの
スクリーンショットはファイルに残らないので当てにしない。CLAUDE.md 10節-21 と `ui_rules.md` 9 にも同じ内容。

### 決定: 本人のスクリーンショットは記事に勝手に使ってよい（本人）
`Pictures/Screenpresso` から BBB のものだけ拾う。本業の 3D 作業のスクショと、出どころ不明の参考画像は載せない。

### 決定: 記事の時間軸は「9/10 に書き始めた」を固定点にする
「今日」を基準にすると更新のたびに全数字が動く。増えた失敗は既存番号を振り直さず末尾に足す（⑳〜㉘）。

### 決定: 告知（エンゲージ / BIG / REG）は絵に差し替えられる形にする。値は zoneFx
文字の見出しを `assets/logo/engage`・`assets/logo/bonus` の絵に置き換えたい、という本人の依頼（2026-09-16）。
`game_config.json` の `zoneFx` に 3 面ぶん（絵・位置・大きさ・出方・帯・光・揺れ・飛び散り）を持ち、
`ZoneFx.Play` が出す。`image` が空なら従来の文字のままなので、決まるまで見た目は変わらない。
決めるのは `tools/zone_viewer.html`（実機の描き出しを背景にして舞台 1170×540 の上で合わせる）。
絵は `tools/ui/install_zone_art.py` が Resources/Art/UI/Zones へ入れる。

### 決定: 宝箱は当選してから 2〜4 G の前兆を挟んで見つかる（本人 2026-09-21）
敵の前兆と同じく「当たっているかも」の間を作る。G 数は `adventure.treasurePrecursorMin / Max`（0〜0 で即発見）。
前兆中は新しい宝箱を抽選しない。章クリアで残っていた前兆は捨てる。
前兆の見せ方は足元のきらめき（段階で増える）と SE `treasure_hint`（段階で高く）、文は `_hint`。

### 決定: ベル択ナビは 2 択の間に心臓音、連続正解で「n COMBO!」（本人 2026-09-21）
心臓音は `navi_choice`（ループ。決めた瞬間に止める）。COMBO は 2 連続から、失敗で 0。値は `reelFx.naviCombo`。

### 決定: ENEMY エンゲージをターン制にする（本人 2026-09-21。案A）
「3G で毎 G 討伐抽選」をやめ、1 ターン目で構え・2 ターン目で結果の 2 G × 3 セット。表は `game_config.json` の `engage`、判定は `Engage.cs`（純粋関数）。
本人の答え: LIFE は左上のライフで 3 減る / 「攻撃確定でハズレ → 小攻撃 or ミス」は無し / ポーションはエンゲージ専用で「追加 1 回転 → ルーレット」。
ルーレットの中身（ハズレ LIFE +3 / 小役 次が大攻撃 / レア役 討伐確定）は私の案を本人が承認。
案B（敵 HP を削る）は取らなかった: 数値が表 1 枚で済み、演出ビューアで見せやすいため。
テクニックの「エンゲージ +G」は 2 G で 1 セットに読み替え（端数切り捨て）。`bellCommand.successGuaranteesDefeat` は使わなくなった（ナビ正解 = レア役扱い、`engage.naviSuccessIsRare`）。

**覆した（同日）**: 本人が実機で試し「2 ターン目で終わった（3 セットやらなかった）」。討伐率で即終了する案A のテンポは本人の期待と違ったので
**案3（敵 HP 制）**に変更。攻撃の大きさ = ダメージ、HP 0 でその場で討伐。3 セットで削り切れなければ**ジャッジの 1 G**（削った割合 + 役の上乗せで倒すか逃げるか）。
討伐率の数字（35 / 70 / 50）はそのままダメージに読み替えた（HP 100 なので同じ感覚で調整できる）。

### 決定: ボタンのホバーは「本体の形に白を薄く重ねる」（本人 2026-09-21「うっすらハイライト」）
ColorBlock の highlightedColor は絵の枠に効かない（白より明るくできない）。同じ絵を白で重ねても絵の色が乗るだけで明るくならない。
なので本体に Mask を付け、その子に白（HoverGlow.Alpha = 0.035）を敷く。リニア色空間なので 0.07 では AUTO が灰色に見えた。
根が絵のボタン（AtelierUi / MapUiV2）は縁の飾りを隠さないよう Mask を使わず、内側の角丸の白。描き出しは tools/hover-qa。

### 決定: 店は「工房」。図面をソウルと素材で作り、作った装備は恒久・＋ボーナス無し（本人 2026-09-21。案C + 案A' の混合）
本人の言葉: 「基本的に作るものは＋ボーナスがつかない。ダンジョンで拾う装備は＋ボーナスが付くことがある」。
拾い物（接辞つき、帰還で流れる）を主役に残しつつ、工房の品で底上げする。案B（店売りも流れる）は取らなかった（Diablo 3 の店の死に方）。
50 の図面は `docs/shop_equipment_plan.md`。素材 4 種（鉄鉱 / 獣の革 / 魔石 / 竜の鱗）は落とし物の kind "material" で財布に貯まる。
工房の品は EquipItem.crafted。鞄の数に入らず、売れず、帰還（EndRun → ClearFound）で残る。段 IV は 3 章の踏破で解放。
値段の相場（1 潜行 100〜300 ソウル）は仮。速すぎる・遅すぎるは値段と解放章で刻む。

### 決定: 序章「灯の落ちた夜」（本人 2026-09-21）
本人の骨組み: 強いボスから逃げてボロボロ → ステージで LIFE が 0 になり倒れる → 暗転で誰かが助ける → 宿屋で「私……どうして？」→ 街を探索してチュートリアル。
逃げた相手 = 第 1 章の終点の竜（本人は覚えていない）。助けた者 = 第 3 章の「灯を持たない旅人」。どちらも本人が承認。
台詞は `story.op`（8 場面 56 行）。場面 2 は 4 回転ぜんぶハズレで灯が尽きる（敵に殴られる形は入れていない。敵の出現が乱数で、5 分に収まらないため）。
街の探索は台詞で 3 か所を回る形（画面の移動はしない）。本物の街の探索にするなら MapScreen に「行ける場所を絞る」モードが要る。

---

## 2026-09-25 エフェクトの見本

### 決定: エフェクトは見本動画で先に決め、合格したものだけ本体へ移す（本人）
本体の UI は Canvas の ScreenSpaceOverlay なので、ParticleSystem をそのまま載せられない。
載せる土台を先に作ると見た目の判断が後回しになるため、別の作業用コピー（本体は触らない）で描き出して見せる。
動画は `docs/art/2026-09-25/`。見本の一覧・本体に要るものの棚卸し・次の手順は `docs/FX_EFFECTS.md`。

### 決定: 画風は項目ごとに使い分ける（本人が選択、割り振りは私）
斬撃・流線・ネガ反転はアニメ寄り（三階調・コマ落ち 30fps / 流線は 20fps）。コイン・オーラはリアル寄り（60fps・ブルーム）。
火花の見本（実写寄り）は本人が「いいじゃん」。

### 決定: 見本全体の強弱を上げる。背景を沈めて光を立てる（2026-09-26 本人「強弱が全然足りない」）
光る物を明るくするだけでは画面全体が白く飛ぶ。一撃の瞬間は光っていない所を暗くして差を作る。
沈める量は画面の光の量で自動に決める（手で項目ごとに付けない）。色は残す（明るさだけ縮めるトーンマップ）。
数値と測り方は `docs/FX_EFFECTS.md`。本体に移すときも同じ考え方で URP のポストを組む。
→ 同日覆した: 本人「ただ強くしただけ。見づらい。かっこよさをわかっていない。ノウハウとデータを集めて再現して」。
全体を強くする方向はやめ、調べた決まり（`docs/FX_RESEARCH.md`）で作り直した。

### 決定: エフェクトは「光らせる所を絞り、周りを沈め、時間で見せる」（2026-09-26）
光るのは細い芯だけ。本体色は光らせない。背景は一撃の間だけ暗くする。
かっこよさは明るさより時間で出す: 予備 → 一撃 → ヒットストップ（格ゲーの実測値）→ 余韻。
一撃の強さは 4 段の `Impact`、発動は `Surge` に束ねる。本体に移すときもこの 2 つを単位にする。

### 決定: エフェクトの素材は既製の CC0 素材から選ぶ（2026-09-26 本人が案 1 を選択）
本人「素材がまず悪い」。コードで描いた図形では表情が出ない。Kenney Particle Pack（CC0・80 枚）から 12 枚を選び、白＋アルファにして使う。
足りない形（連番の炎・斬撃の繊維など）が出たら、ChatGPT 画像かコードで補う。

---

## 検証の記録

### 2026-09-14 C-1を新画風の3層にしてUnityへ接続

本人「近づいているので進めてください」でv2の方向で継続。「続きをお願い」で中断していた検証も再開。
遠景・中景・近景を別々に生成したが、実アルファの指示は2回ともRGBで返った。チェック柄を透過と誤認せず不採用にし、黒マットRGBをUnityの専用シェーダーで透過する構成へ。左右の自然な空白と連続した地面を組み合わせ、異なる幹を重ねてつなぐ方法はC-1では使わない。中立の地形色、時間の色、雨なしのCloudyを分離した。
実描画で黒い縁と平坦な足元が目立ったため、輪郭の近傍だけ黒マットを補正し、足元を連続した色面の模様へ変更。旧29地点と確率・報酬の処理は維持。
確認: Unity 6000.3.15f1の隔離コピー、Built-inカメラのuGUI実描画。30地点の読み込み、C-1の4時間×晴れ曇り雨、3層それぞれ正逆3回の周期境界、GameControllerの2解像度。C-1の最大平均RGB境界差0.0002013812。EditModeは102件成功／失敗0／スキップ0、UI配置検算も成功。URIや時刻の差で古い検証結果と混同しないよう、実行スクリプトと結果を `tools/environment-qa/` に保存。
制限: 本体のURP Play Mode、端末実機、GPU負荷、配布ビルドと旧29地点の新画風化は未実施。初回起動のライセンス接続制限と一時manifestのBOMを解消。再起動が自動承認レビューの利用上限で一度拒否されたが、本人の続行依頼後に再実行し成功。
正本は `docs/BACKGROUND_ART_DIRECTION.md`。次はA-1の開けた風景、E-2とH-1の暗所・発光を確認して展開する。

### 2026-09-14 背景の画風見直しとMDでの引き継ぎ

本人は「シームレスな横スクロール」「Unityで朝昼夕夜・雨・曇り等を変更」「今よりアニメチック」「まずテイスト選定」「他のセクションでも常にMDにまとめる」と指定。
現行は30地点の3層アトラスと時間・8天候。左右16%の重ね合わせで周期をつなぎ、独立した曇りは未実装とソースで確認。
組み込みimage_genで同じ草原のAセル画／B手描きアニメ／C絵本を比較するボードを作成。本人は「少しテイストが違うので参考画像をお渡しします」と回答。初回3案は採用せず、参考画像受領後に作り直す。
制作方針案は各層自身の左右連続、空・雲・天候・灯の発光の分離、代表1地点の12条件検証から展開。画風の正本は `docs/BACKGROUND_ART_DIRECTION.md`、画像とプロンプトは `docs/art/backgrounds/2026-09-14/`。
→ 同日追記: 本人が紫の遺跡・青い洞窟・緑の森の3参考画像を提供し「こんな感じです」。参考をプロジェクトに保存し、色面・硬い筆致・深い影・鮮やかな光を基準にC-1の横長見本v2（2172×724）を生成。初回B推奨は基準にしない。v2は画風確認用で、本人の採用判断待ち。最初の層分離試作は比較しやすいC-1、その後A-1に変更する提案を記録。
確認はソース・画像の目視と記録の整合。今回は本番素材・Unityコードを変更しておらず、新背景の実描画・シームレス検証は未実施。開始時の30枚のPNG `.meta` 変更は保持。ルートAGENTS.mdに全セクション共通のMD更新手順を追加。

数字は「いつ・どの条件で測ったか」とセットで残す。条件が違うと比較できない。

| 日付 | 何を | 条件 | 結果 |
|---|---|---|---|
| 09-08 | リール制御 | 全23フラグ × 押し順6 × 押し位置20³ = 1,104,000 | ルール違反 0、順押しで必須役 100% |
| 09-09 | 機械割 | 設定1 / 200万G / ナビ従 | 132.6%（20万G では 128.8〜134.1% と揺れた） |
| 09-09 | 機械割 | 設定1 / 200万G / 順押し | 107.7%（押し方で 25pt 変わる） |
| 09-10 | 技術介入 | 20万G / 狙って押す | 発生 5.0%/G、成功 100% |
| 09-10 | 技術介入 | 20万G / 適当に押す | 成功 51.6%（ビタ押しは 4〜6%） |
| 09-10 | エンバー収支 | 20万G / 狙って押す | 1000G あたり 696（回復薬 17 個ぶん） |
| 09-10 | HP 収支 | 20万G / 設定1 / 回復率30%・回復量8 | ボーナスの回復が通常時の減りの 50.1% を戻す |
| 09-10 | HP の不変条件 | 20万G / 通常2万G・非通常3.9万G | 通常時は 1G で必ず 1 減り、ボーナス・AT・持ち越し中は減らない（違反 0） |
| 09-12 | ボーナスの長さ | 冒険なし / 各 200 回 / ベル ナビ60%・11/3/8 | 旧一律15: BIG 36G・REG 34G → 今: BIG 67G（順押し 137G）・REG ナビ従 133G・順押しは 1500G でも終わらず。設定5 は REG が両方終わらず |
| 09-12 | G 数管理のボーナス | 冒険なし / 各 300 回 / BIG 60G・REG 30G | BIG 59G 純増 +271（順押し +133）/ REG 30G 純増 +23（順押し −12、設定5 は −27） |
| 09-14 | REG のベル 65% | 冒険なし / 300 回 / RB 表のベル 36.6% → 65%（全設定） | REG ナビ従 25G 純増 +88（規定 90 枚で終わる回 72%）/ 順押し 30G +45。機械割 107.6% → 108.4%（20 万G） |
| 09-15 | REG の規定枚数 110 | 冒険なし / 300 回 / payouts.REG 90 → 110（棚 c09） | REG ナビ従 28G 純増 +101（規定枚数で終わる回 55%）/ 順押し 30G +44。機械割 108.3% → 109.3%（20 万G・設定 1） |

---

## 失敗から学んだこと

`Claude-ExternalBrain/observations/OBSERVATIONS.md` に詳細がある。要点だけ:

- **資源制を入れたら、実際に尽きる頻度を測るまで実装は終わっていない**（O-014）。
  松明を入れた初回は 25万G 回して 1 度も尽きなかった
- **状態機械に新しいモードを足したら、既存モードとの同時成立を総当たりで検査する**（O-012）。
  AT と通常時のエンゲージが同時に出る事故が 100万G 中 558 回起きていた
- **画面更新の関数の中で例外が出ると、その後ろが全部止まる**（O-015）。
  症状が 2 つ以上あって原因が別々に見えるときは、共通して通る 1 本の関数を疑う
- **自分で書いたレビュー用のルールは、機械が実行できる形にしないと自分が破る**（O-016）
- **Json.NET は既定値入りの List に JSON を読むと追記する**（O-011）。
  モンスター 3 体の JSON が既定 3 体と合わさり 6 体になっていた

### 決定: エンゲージ中のベル択ナビの正解は「ただの小役」（本人 2026-09-23）
レア役扱い（`engage.naviSuccessIsRare`）をやめた。値は false。覆した: 2026-09-21 の「ナビ正解 = レア役扱い」。

### 決定: ジャッジの G は BET で「JUDGE」のカットイン → 第一〜第三停止で段階的に（本人 2026-09-23）
本人「EE 時ジャッジの 1G がない。BET 時に JUDGE と文字が出てカットイン演出が始まる」「第一停止〜第三停止でいろんな演出が見られるように」。
ジャッジの G は Core が動いていたが、停止後に小さく「JUDGE n%」が出るだけで、1 G として見えていなかった。
結果（倒す / 逃げる）はレバーで決め（RollJudgeAtLever）、熱さ（青 / 黄 / 赤 / 虹）を結果で重みを変えて選ぶ。第一停止 = 前触れの一言、第二停止 = 熱さの帯、第三停止 = 決着。
値は `engage.judgeFx`、演出ビューアの「ジャッジの演出」。

### 決定: 技術介入は「引いた小役ごとに決まったリールで、BAR か 7 を枠内か枠のすぐ上下に」（本人 2026-09-24）
本人の言葉: 小役の一部で発生。法則性は決まっていて、ナビとリールの図柄を一緒にしたい。チェリーなら左リールに必ず（チェリーと判断せずに他を押すと取りこぼしやすい）。止める位置は必ず BAR か 7 が枠内か、枠より 1 個はみ出る位置。リプレイは出さない、ベルは出せるはず。
決まり（`tech.roleRules`）: チェリー = 左 30% / スイカ = 中 20% / ベル = 右 5%（スイカとベルのリールと率は私の仮置き）。成功 = 役の図柄が枠に入り、狙いの図柄が枠内か上段の 1 つ上・下段の 1 つ下（`TechDirector.FrameTops`）。
左リールでは、チェリー 15 番を上段 → BAR 枠内、14 番 → BAR 枠のすぐ下、1 番 → 赤7 枠のすぐ下 の 3 つになる。場面ごとの率と levels のランダムは roleRules があれば使わない。
測った値（2026-09-24、ハーネス 20 万G、設定 1）: 発生 1.43%。狙いの位置で押したときの成功 93%（中・右リールの課題で、先に他のリールを押すと制御の滑りで外れることがある）。

# docs/note — 販売用note（制作過程）の入口

**新しいセッションはこの1枚を読めば続きから書ける。** `/clear` の後もここから入る。

---

## 0. これは何か

BigBonusBlitz を AI に作らせた過程を、有料note として売るための原稿一式。

- 売り方: 単発の有料note。980〜1,500円（本人決定・2026-09-10）
- 構造: B（AIに作らせ続ける仕組み）+ C（壊れた箇所の全開示）の混合（本人決定・2026-09-10）
- 読者: 非エンジニア。コードを書かない人（本人決定・2026-09-10）
- **公開のタイミング: ゲームの完成と同時**（本人決定・2026-09-10）。それまでは書き足し続ける

## 1. ファイルの役割

| ファイル | 役割 | 誰が触るか |
|---|---|---|
| `README.md` | この入口。方針と次の1手 | 方針が変わったときだけ |
| `note-01-ai-game-making.md` | 記事の本体。これを note に貼る | 節目でまとめて更新 |
| `MATERIAL.md` | 記事のどの節にどの材料が入っているかの割り当て表 | 記事に反映したとき |
| `MEMO.md` | 本人のメモの原文。記事に「そのときのメモ」として混ぜる | **本人が何か言ったら即追記** |
| `images/` | note に貼る画像10枚と、足りない分のショットリスト | 画像を足すとき |
| `build_images.py` | 画像の生成スクリプト。`py -3 docs/note/build_images.py` | 図解を直すとき |
| `build_rig_images.py` | タイトル立ち絵（SaliaRig）から 19・20 を合成 | 立ち絵が変わったとき |
| `insert_images.py` | 記事への差し込み。実行済み（再実行しても二重に入らない） | 基本さわらない |
| `patch_v2.py` | devlog の材料を反映した第2稿への更新。実行済み | 基本さわらない |

**原則: 気づいたことは `MATERIAL.md` に足す。記事本体は節目にまとめて直す。**

記事本体を毎回いじると、文体と構成が崩れる。材料だけ先に貯める。

## 2. 毎セッションやること（BBB の作業をしたら必ず）

BBB のコードや仕様を触ったセッションは、終わりに次を1回やる。

記録の本体は次の3つ。**note 用に書き写さない。**

- `docs/devlog.md` … 決定と理由。検証した数字（いつ・どの条件で測ったか）
- 外部知能 `errors/ERRORS.md` … 効かなかった方法と効いた方法
- 外部知能 `observations/OBSERVATIONS.md` … 昇格待ちの発見

`MATERIAL.md` に足すのは、**記事にまだ入れていない材料の控えだけ**（1件2行）。
出どころは上の3つのどこかを指す。

## 3. 記事本体を更新するタイミング

次のどれかが起きたときだけ、`note-01-ai-game-making.md` に反映する。

- `MATERIAL.md` の未整理が10件を超えた
- 章の完成・リリースなど、ゲーム側で節目があった
- 本人から「記事を更新して」と言われた

更新の手順:

1. `MATERIAL.md` の未整理を読み、記事のどの節に入るか割り当てる
2. 該当節だけ書き足す。他の節は触らない
3. 「壊れた箇所カタログ」に足したら、**件数を全部の箇所で直す**（タイトル・目次・型の分類・締め）
4. `skills/story-sales-copy/references/review.md` の4パスを回す
5. `MATERIAL.md` の該当行を「反映済み」へ移す

## 4. 記事の現在地

- 状態: 第2稿（2026-09-11）。約23,000字。devlog の材料と本人メモ2本を反映済み
- 壊れた箇所カタログ: **19件**（ゲーム側13 / AI運用側6）。型は5つ（A=11 / B=2 / C=3 / D=2 / E=1）
- 作業時間: 平日1日2時間ほど（本人の実感値）。実際に触っていた期間は約50日。記事に記載済み
- 未確定: 費用、公開日、価格の最終決定
- 記事内で `[数字を入れる]` と書いた箇所はなし
- 画像: 13枚を差し込み済み。Unity 実機の画面は 21（新タイトル）の1枚。不足分は `images/README.md` の2節。次の優先は 11（スロット本編）

## 5. 次の1手

本人待ちは現在なし。

1. **AI側・毎回**: BBB を触るたびに `MATERIAL.md` へ追記
2. **AI側・完成時**: 「9. 今も直っていないもの」を現況に合わせて全面差し替え
3. **AI側・完成時**: 壊れた箇所の件数を、タイトル・目次・型の分類・締めで揃え直す
4. **本人・公開前**: 外部知能リポジトリの掲載範囲を最終確認（載せる方針で確定済み。最後に読み直すだけ）

## 6. 書くときの決めごと

- 一人称は「私」。文は短く切る。箇条書きを多用する
- 数字は捏造しない。出どころを言えない数字は書かない
- 「必ず作れる」「誰でもできる」と書かない。事例1件と再現性を混同しない
- 引用と実測を混ぜない（記事中の失敗⑩がまさにこれ）
- 太字は節あたり2〜3箇所まで。強調は `"…"` を使う
- 技術用語は必ず言い換える。読者はコードを書かない
- 本人のメモは `MEMO.md` に原文で置き、記事では `>` 引用＋「そのときのメモ（日付）」の見出しで入れる。AI が言い換えない

## 7. 関連する場所

- ゲーム側の現状: `docs/PROJECT_STATE.md`
- 失敗の一次記録: 外部知能 `errors/ERRORS.md`
- 昇格済みルール: 外部知能 `rules/RULES.md`
- 案件の現在地とタスク: 外部知能 `projects/note-bbb.md` / `note-bbb.tasks.json`
- 文体スキル: `skills/story-sales-copy`
